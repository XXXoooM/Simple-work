{
  "name": "department-orders-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute order-db --file=./schema.sql --local",
    "db:init:remote": "wrangler d1 execute order-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "itty-router": "^5.0.0"
  }
}
